Hooks Git deste repositorio.

A pasta `.githooks` e versionada e pode ficar sem scripts executaveis.

Apos clonar, na raiz:

  git config core.hooksPath .githooks

Em Windows, se houver problemas com o `git` do PATH, use o executavel instalado pelo Git for Windows (por exemplo em `Program Files\Git\cmd`).
